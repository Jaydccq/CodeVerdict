# Merge Primary and Secondary Branches Minimum Conflicts

The longest common subsequence is the largest clean merge backbone. Commits outside that backbone are counted as conflicts.

Reference complexity depends on the stated approach and input bounds. The generated fixtures in this repository are authored practice tests.
