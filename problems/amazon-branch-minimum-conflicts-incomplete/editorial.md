# Merging Branch Minimum Commit Conflict Count

The cleanly shared commits form a longest common subsequence. All other commits are conflicts, so the answer is m + n - 2 * LCS.

Reference complexity depends on the stated approach and input bounds. The generated fixtures in this repository are authored practice tests.
