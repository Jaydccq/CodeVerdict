# Maximum Money by Taking K Consecutive Bags

Maintain a sliding window of length k. Move the window one position at a time by adding the entering value and removing the leaving value.

Reference complexity depends on the stated approach and input bounds. The generated fixtures in this repository are authored practice tests.
