# Backend API Test Suite Repository Debug

Validate request payloads before saving, return 404 for missing records, and assign the updated status before persisting it.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
