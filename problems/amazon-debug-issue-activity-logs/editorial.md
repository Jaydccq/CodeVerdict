# Issue Activity Log Repository Debug

Include COMMENT in the supported action set, reject duplicate IDs in the repository, and sort summaries before returning them.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
