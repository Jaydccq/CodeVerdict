# Workflow Sub-Issue Repository Debug

Validate the parent exists, reject duplicate child IDs, and assign then persist the parent's incremented childCount.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
