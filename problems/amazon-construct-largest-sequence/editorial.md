# Construct Lexicographically Largest Sequence

The optimal next element is always the largest currently unlocked unused value. Use a max heap in production; the reference generator scans because fixtures are small.

Reference complexity depends on the stated approach and input bounds. The generated fixtures in this repository are authored practice tests.
