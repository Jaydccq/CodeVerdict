# Drone Delivery Circular Hub Minimum Travel Time

Precompute prefix sums around the cycle. For each consecutive pair, compute the distance along one arc and subtract from the total cycle length to get the opposite arc, then add the smaller value.

Reference complexity depends on the stated approach and input bounds. The generated fixtures in this repository are authored practice tests.
