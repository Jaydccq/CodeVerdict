# MovieDB Recommendation Repository Debug

Filter out watched movies, use >= for the rating threshold, require any genre overlap, and sort by rating descending with title as tie-breaker.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
