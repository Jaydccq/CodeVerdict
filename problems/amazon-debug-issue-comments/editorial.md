# Issue Comment Platform Repository Debug

Reject empty or whitespace-only bodies, assign deleted state during deletion, and filter deleted comments out of active counts.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
