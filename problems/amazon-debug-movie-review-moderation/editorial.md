# Movie Review Moderation Repository Debug

Normalize review text before banned-word checks, reject once accepted reviews are already at the limit, and persist only accepted reviews.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
