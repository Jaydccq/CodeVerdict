# Password Reset Token Repository Debug

Check revoked state, use now <= expiresAt, and assign used = true before persisting token consumption.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
