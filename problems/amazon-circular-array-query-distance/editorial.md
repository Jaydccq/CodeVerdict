# Circular Array Prefix Sum Multi-query Distance

Prefix sums turn each clockwise arc into O(1). The counterclockwise arc is total minus clockwise; add the smaller one for every query.

Reference complexity depends on the stated approach and input bounds. The generated fixtures in this repository are authored practice tests.
