# Banking RBAC Repository Debug

Check the role first. If it is missing, deny. Then allow only when the role has "*" or the exact requested permission.

The repository tests are deterministic practice tests. Fix the implementation instead of changing test files.
