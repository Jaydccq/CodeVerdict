# Maximum System Memory Capacity with Primary and Backup Servers

Consider primary capacities from largest to smallest and assign the smallest backup that can cover each chosen primary. This preserves large backups for only when needed.

Reference complexity depends on the stated approach and input bounds. The generated fixtures in this repository are authored practice tests.
